<Page x:Class="CeMCP.ConfigPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CeMCP"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ConfigPage"
      x:Name="ConfigPageRoot">
    
    <Page.Resources>
        <Style x:Key="DarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
        </Style>
        
        <Style x:Key="LightTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#AAAAAA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
        </Style>
        
        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#444444"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#666666"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <Style x:Key="LightButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#AAAAAA"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Page.Resources>

    <Grid x:Name="MainGrid" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="TitleTextBlock" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="MCP Server Configuration" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

        <Label x:Name="HostLabel" Grid.Row="1" Grid.Column="0" Content="Host/IP:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="HostTextBox" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}" Margin="5,2"/>

        <Label x:Name="PortLabel" Grid.Row="2" Grid.Column="0" Content="Port:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="PortTextBox" Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Margin="5,2"/>

        <Label x:Name="ServerNameLabel" Grid.Row="3" Grid.Column="0" Content="Server Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="ServerNameTextBox" Text="{Binding ServerName, UpdateSourceTrigger=PropertyChanged}" Margin="5,2"/>

        <Label x:Name="BaseUrlLabel" Grid.Row="4" Grid.Column="0" Content="Base URL:" VerticalAlignment="Center"/>
        <TextBlock x:Name="BaseUrlTextBlock" Grid.Row="4" Grid.Column="1" Text="{Binding BaseUrl}" VerticalAlignment="Center" Margin="5,2" FontFamily="Consolas"/>

        <Label x:Name="StatusLabel" Grid.Row="5" Grid.Column="0" Content="Status:" VerticalAlignment="Center"/>
        <TextBlock x:Name="StatusTextBlock" Grid.Row="5" Grid.Column="1" Text="{Binding ServerStatus}" Foreground="{Binding ServerStatusColor}" VerticalAlignment="Center" Margin="5,2" FontWeight="Bold"/>

        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,20,0,10">
            <Button x:Name="SaveButton" Content="Save Configuration" Click="SaveButton_Click" Margin="0,0,10,0" Padding="10,5"/>
            <Button x:Name="TestButton" Content="Test Connection" Click="TestButton_Click" Margin="0,0,10,0" Padding="10,5"/>
            <Button x:Name="StartStopButton" Content="{Binding StartStopButtonText}" Click="StartStopButton_Click" Padding="10,5"/>
        </StackPanel>

        <TextBlock x:Name="TestResultTextBlock" Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding TestResult}" TextWrapping="Wrap" Margin="0,10,0,0" FontFamily="Consolas" FontSize="11"/>
    </Grid>
</Page>
